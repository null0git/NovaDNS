[project]
name = "novadns"
version = "1.0.0"
description = "A modern, self-hosted DNS server and management platform built with Python."
requires-python = ">=3.10"

dependencies = [
    "Flask>=3.0",
    "psutil>=5.9",
    "cryptography>=41.0",
]

[tool.vercel]
entrypoint = "run:app"
